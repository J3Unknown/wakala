import 'package:flutter/material.dart';

class AddAuctionAlert extends StatelessWidget {
  const AddAuctionAlert({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
